name: pf-publication-identity-sanitization

on:
  push:
    branches:
      - feat/pf-publication-inline-approval-curation
    paths:
      - tools/patch_pf_publication_identity_sanitization.py
      - .github/workflows/pf-publication-identity-sanitization.yml

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feat/pf-publication-inline-approval-curation
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Apply guarded patch
        run: python tools/patch_pf_publication_identity_sanitization.py
      - name: Commit patch
        run: |
          if git diff --quiet; then
            echo "No changes to commit"
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add services/api/src/routes/public_perfect_fit_admin.js
          git commit -m "Sanitize PF moderation product identity"
          git push origin HEAD:feat/pf-publication-inline-approval-curation
